# Retrieval Evaluation

- Index a tiny fixture with known symbols and dependencies.
- Query by exact identifier, natural language description, and nearby concept.
- Assert stable ordering only when the scoring contract makes it deterministic.
- Track chunk path, line span, score, and text preview in debugging output.

